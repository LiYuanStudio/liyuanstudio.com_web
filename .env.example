# Local dev: Vite proxies /api to the backend, so use a relative path.
# Production: set the full deployed API URL, e.g. https://your-api-domain.com/api
VITE_API_BASE_URL=/api
VITE_PUBLIC_PROFILE_PREFIX=/
