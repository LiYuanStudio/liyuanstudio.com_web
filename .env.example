# Local dev: Vite proxies /api to the backend, so use a relative path.
# Production: set the full deployed API URL, e.g. https://your-api-domain.com/api
VITE_API_BASE_URL=/api
# Temporary production-only bridge for migrating legacy HttpOnly JWT cookies.
# VITE_LEGACY_API_BASE_URL=https://liyuanstudio-com-web.vercel.app/api
